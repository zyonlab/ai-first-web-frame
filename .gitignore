node_modules/
dist/
dist-browser/
.next/
coverage/
playwright-report/
test-results/
reports/playwright/
reports/traces/
.DS_Store

# C3 spike (docs/ARCHITECTURE_REFACTOR_PLAN.md §4.3.3): generated shared
# vendor chunk build output — regenerated by `pnpm --filter @mvp/page-trade
# run build:spike-vendor` (chained into that package's `build` script), not
# checked in, same convention as `dist/`.
apps/page-trade/public/spike-vendor/
