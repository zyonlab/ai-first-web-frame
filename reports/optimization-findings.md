# Optimization Findings

Status: warn

| Severity | Category | Target | Message | Recommendation |
| --- | --- | --- | --- | --- |
| info | ssg | recommendations | Slot "recommendations" has no declared dependencies and may not need dynamic SSR. | Change the slot strategy to static, isr, or cached-ssr if its content is deterministic. |
| info | ssg | recommendations | Slot "recommendations" has no declared dependencies and may not need dynamic SSR. | Change the slot strategy to static, isr, or cached-ssr if its content is deterministic. |
| info | ssg | price-panel | Slot "price-panel" has no declared dependencies and may not need dynamic SSR. | Change the slot strategy to static, isr, or cached-ssr if its content is deterministic. |
