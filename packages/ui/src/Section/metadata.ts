import { componentBudget, metadata } from "../factory";

export const sectionMetadata = metadata(
  "Section",
  "Semantic server-safe content section",
);
export const sectionBudget = componentBudget("Section");
