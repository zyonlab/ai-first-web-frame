import { componentBudget, metadata } from "../factory";

export const skeletonMetadata = metadata(
  "Skeleton",
  "Server-safe loading placeholder",
);
export const skeletonBudget = componentBudget("Skeleton");
