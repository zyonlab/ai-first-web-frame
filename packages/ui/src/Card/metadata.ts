import { componentBudget, metadata } from "../factory";

export const cardMetadata = metadata(
  "Card",
  "Server-safe framed content container",
);
export const cardBudget = componentBudget("Card");
