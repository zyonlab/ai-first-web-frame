import { componentBudget, metadata } from "../factory";

export const productCardBaseMetadata = metadata(
  "ProductCardBase",
  "SEO-safe product summary card",
);
export const productCardBaseBudget = componentBudget("ProductCardBase");
