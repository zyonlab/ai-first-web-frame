{
  "name": "@mvp/fragment-positions-table",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "exports": {
    "./patch": "./src/patch.ts"
  },
  "scripts": {
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "build": "tsdown src/server.ts --format esm --platform node --dts",
    "test": "pnpm -w exec vitest run fragments/positions-table/tests fragments/positions-table/src",
    "test:watch": "pnpm -w exec vitest fragments/positions-table/tests fragments/positions-table/src",
    "typecheck": "pnpm exec tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mvp/contracts": "workspace:*",
    "@mvp/data": "workspace:*",
    "@mvp/interaction": "workspace:*",
    "@mvp/observability": "workspace:*",
    "@mvp/request-context": "workspace:*",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsdown": "^0.11.0"
  }
}
