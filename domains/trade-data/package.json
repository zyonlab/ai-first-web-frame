{
  "name": "@mvp/trade-data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "pnpm -w exec vitest run domains/trade-data/src",
    "test:watch": "pnpm -w exec vitest domains/trade-data/src",
    "build": "tsdown src/index.ts --format esm --dts",
    "typecheck": "pnpm exec tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mvp/contracts": "workspace:*",
    "@mvp/data": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "^0.11.0"
  }
}
